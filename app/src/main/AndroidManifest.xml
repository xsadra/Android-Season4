<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.sadra.apps.season4java">

    <application
        android:name="app.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".E01AndroidArchitectureActivity" />
        <activity android:name=".E02AndroidLifeCycleActivity" />
        <activity android:name=".E03AndroidUIControlsActivity" />
        <activity
            android:name=".E04AndroidHexColorsActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_e04_android_hex_colors"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".E05AndroidTextViewActivity" />
        <activity android:name=".E06AndroidButtonActivity" />
        <activity android:name=".E07AndroidEventHandling01">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>